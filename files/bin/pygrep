#!/bin/bash
grep --colour -R --include '*.py' \
    --exclude-dir '.*' \
    --exclude-dir _build \
    --exclude-dir branches \
    --exclude-dir build \
    --exclude-dir debian \
    --exclude-dir forks \
    --exclude-dir target \
    --exclude-dir venv \
    "$@" ~/[a-z]*
